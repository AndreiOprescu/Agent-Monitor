{
  "name": "agent-monitor-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "jest --forceExit --testTimeout=10000",
    "build": "node --check server.js",
    "lint": "eslint server.js '__tests__/**/*.js'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
